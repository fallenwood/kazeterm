name: CI

on:
  push:
    branches: [ master ]

jobs:
  build-release:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, windows-11-arm, ubuntu-24.04-arm, macos-latest, macos-26-intel]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98

      - name: Cache
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Setup
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -y && sudo apt install -y \
            clang \
            llvm \
            libxcb1-dev \
            libxkbcommon-dev \
            libxkbcommon-x11-dev \
            zstd

      - name: Build
        run: cargo build --profile=release-fast

      - name: Install cargo-llvm-cov
        run: cargo install cargo-llvm-cov --locked

      - name: Run coverage (tests included)
        if: ${{ !(runner.os == 'Windows' && runner.arch == 'ARM64') }}
        run: |
          mkdir -p ./coverage
          cargo llvm-cov --workspace --all-features --lcov --output-path coverage/lcov.info

      - name: Upload coverage artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: runner.os == 'Linux' && runner.arch == 'X64'
        with:
          name: coverage-lcov
          path: coverage/lcov.info

      - name: Pack
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          mkdir ./dists
          mv target\release-fast\kazeterm.exe ./kazeterm.exe
          $variant = "${{ runner.os}}-${{ runner.arch }}".ToLower()
          7z a ./dists/kazeterm-$variant-${{ github.ref_name }}.7z ./kazeterm.exe

      - name: Pack
        if: runner.os == 'Linux'
        shell: pwsh
        run: |
          mkdir -p ./dists
          $variant = "${{ runner.os}}-${{ runner.arch }}".ToLower()
          tar --zstd -cf ./dists/kazeterm-$variant-${{ github.ref_name }}.tar.zstd -C ./target/release-fast kazeterm

      - name: Install cargo-bundle
        if: runner.os == 'macOS'
        run: cargo install cargo-bundle --locked

      - name: Bundle macOS DMG
        if: runner.os == 'macOS'
        run: cargo bundle --profile=release-fast --format=osx -p kazeterm

      - name: Pack
        if: runner.os == 'macOS'
        shell: pwsh
        run: |
          mkdir -p ./dists
          $variant = "${{ runner.os}}-${{ runner.arch }}".ToLower()
          tar -czf ./dists/kazeterm-$variant-${{ github.ref_name }}.tar.gz -C ./target/release-fast kazeterm
          tar -czf ./dists/kazeterm-$variant-${{ github.ref_name }}-dmg.tar.gz -C ./target/release-fast/bundle/osx Kazeterm.app

      - name: Upload release asset
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda
        with:
          tag_name: wip
          prerelease: true
          files: |
            ./dists/*
